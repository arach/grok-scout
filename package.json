{
  "name": "grok-scout",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "description": "Grok / Grok Bot MCP config and installer for OpenScout project-routed asks.",
  "scripts": {
    "install:global": "node scripts/install.mjs --global",
    "install:project": "node scripts/install.mjs --project .",
    "check": "node --check scripts/install.mjs && node --test scripts/install.test.mjs"
  }
}
